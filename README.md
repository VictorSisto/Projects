# L&A Gestão

Fonte sanitizada da release de estabilização 1.0.1 do sistema de gestão da L&A Serralheria.

A fonte canônica é reconstruída durante o build a partir dos arquivos em `release-payload/`. Nenhuma credencial, `.env`, `node_modules`, cache ou build local está versionado.